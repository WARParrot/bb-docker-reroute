{
  "name": "bb-plugin-docker-route",
  "version": "2.3.0",
  "type": "module",
  "description": "Container-first routing for bb sessions in docker sandboxes: zero-touch self-healing shell bootstrap, env-dir provisioning, wipe-proof self-restore.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/WARParrot/bb-docker-reroute.git"
  },
  "engines": {
    "bb": ">=0.9",
    "bbPluginSdk": ">=0.4.1"
  },
  "bb": {
    "name": "bb Docker Route",
    "description": "Routes bb sessions in docker sandbox containers: self-healing shell bootstrap, env-dir provisioning, wipe-proof self-restore. Zero-touch attachment mirroring into sandbox homes (bb background service + host watcher + CLI).",
    "branding": {
      "icon": "./assets/icon.svg"
    },
    "server": "./server.ts",
    "skills": [
      "skills"
    ]
  },
  "keywords": [
    "bb-plugin",
    "docker",
    "sandbox",
    "container"
  ],
  "scripts": {
    "test": "bash tests/run-tests.sh"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "^0.4.34"
  }
}
